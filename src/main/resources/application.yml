server:
  port: 8080

spring:
  application:
    name: ecommerce-api
  datasource:
    url: jdbc:mysql://localhost:3306/ecommerce_db
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true